[package]
name = "thorium_ebpf-common"
version = "0.1.0"
edition = "2021"

[dependencies]
aya = { version = "0.13.1", optional = true }
aya-ebpf = { version = "0.1.1", optional = true }

[features]
user = ["aya"]
bpf = ["aya-ebpf"]
